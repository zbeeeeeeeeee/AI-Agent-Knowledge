# 开发者文档风格约定

适用：`markdown/**/*.md` 全部正文；根目录索引页按 L1/L2/L3 分层维护。

## 1. 写作原则

1. 面向开发者：直接给定义、步骤、配置、示例、检查项，不写“本文将/本页将/我们接下来”。
2. 一段话说不清就拆成表格、列表或代码，不做情绪铺垫。
3. 每个文档以 **TL;DR** 开头，只写 3–5 条可执行结论。
4. 语言策略只保留一行元信息：`code` 或 `shared`；导航信息放文末“相关文档”。
5. 概念文档写“是什么 / 适用场景 / 不适用场景 / 怎么落地”；实践文档写“目标 / 步骤 / 配置 / 代码 / 验证 / 故障排查”。

## 2. 禁止出现的表达

| 类别 | 示例 | 改为 |
| --- | --- | --- |
| 自指 | 本文将、本页将、本文回答 | 直接写动作或删除 |
| 铺垫 | 众所周知、不难发现、可以看到、其实、说白了 | 删除 |
| 口头过渡 | 也就是说、简单来说、换句话说 | 删除或改“即” |
| 主观语气 | 我们相信、值得注意 | 删除或改为事实/告警 |
| 万能开头 | 为什么需要这一页、使用说明 | `适用场景` / `用法` |

## 3. 固定文档骨架

```text
# 标题（动词优先）
> 语言策略：code（Java / Python / TypeScript）或 shared（Java canonical，P/T 同文）

## TL;DR
## 概念/模型/指标
## 使用步骤
## 代码示例
## 检查清单/故障排查
## 相关文档
```

## 4. 维护规则

- 修改正文后同步更新对应 PDF；
- 新增内容先过本约定再提交；
- 根目录索引由 `build_knowledge_map.py` 生成，不手改。
